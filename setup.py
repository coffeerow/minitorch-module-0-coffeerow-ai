from setuptools import setup

setup(py_modules=[])

setup(
    name="minitorch",
    version="1.0",
    author="coffeerow",
    description="a minimal neural network libary",
    packages=["minitorch"],
)
